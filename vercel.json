{
  "buildCommand": "cd React && npm install && npx vite build",
  "outputDirectory": "React/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.js"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
